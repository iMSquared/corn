# @package _global_

defaults:
  - image_mwm_pretrained
  - ../net/aggregator/gru@net.state.aggregator.img

agent:
  # This configures compute_state() to
  # operate on each frame.
  bptt_horizon: 4
  bptt_burn_in: 0
  bptt_stride: 1
